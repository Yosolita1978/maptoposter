# Web API dependencies. Installs the generator's own deps first, then the
# server bits. Use this file (not requirements.txt) when running the API.
-r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
